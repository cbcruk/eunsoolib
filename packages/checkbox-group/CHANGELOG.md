# @cbcruk/checkbox-group

## 0.1.0

### Minor Changes

- d90540e: 제출 시점에 선택을 받는 `onSubmit`, 확정된 선택이 바뀌었을 때만 호출되는 `onChange`, 제출 버튼 문구를 바꾸는 `submitLabel` prop을 추가하고 `CheckboxGroupProps`·`CheckboxGroupOption` 타입을 export합니다. 디버그용 선택 상태 `<pre>` 출력은 더 이상 렌더링하지 않습니다(breaking). (#19)
