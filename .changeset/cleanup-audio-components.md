---
'@cbcruk/audio-components': patch
---

`formatCount`를 `@cbcruk/utils`로 옮기고 deprecated 재내보내기로 남겼으며, 이제 `999999`를 `'1000.0K'` 대신 `'1.0M'`으로 표시합니다. README에 `CastAudioPlayer`를 쓰려면 Tailwind v4 `@source` 설정이 필요하다는 안내를 추가했습니다. (#19)
